-Type checker
-allocate constant arrays and strings on the heap and do all computations there
-builtin functions
-check that correct number of arguments are passed to function on application
-add support for preprocessor

there might be some trouble with assigning in assignment expressions
for example,
x := (y := 2) + y
